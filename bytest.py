# Get dynamic inputs from the user (No longer fixed numbers)
width = float(input("Enter the width: "))
length = float(input("Enter the length: "))

# Calculate the area
area = width * length

# Display the result
print("--------------------")
print("The area is:", area)
print("--------------------")